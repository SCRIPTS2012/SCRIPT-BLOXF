-- 👾 Void_GUI Universal Auto-Win + AutoLoop (Draggable) 🇻🇳
-- by Void_Team

-- UI
local ScreenGui = Instance.new("ScreenGui")
local Frame = Instance.new("Frame")
local Button = Instance.new("TextButton")
local LoopButton = Instance.new("TextButton")
local TopText = Instance.new("TextLabel")

ScreenGui.Parent = game:GetService("CoreGui")

Frame.Parent = ScreenGui
Frame.BackgroundColor3 = Color3.fromRGB(30,30,30)
Frame.Position = UDim2.new(0.35,0,0.7,0)
Frame.Size = UDim2.new(0,220,0,110)
Frame.Active = true
Frame.Draggable = true

TopText.Parent = ScreenGui
TopText.Text = "by 🇻Void_Team🇻"
TopText.TextColor3 = Color3.fromRGB(0,255,255)
TopText.TextStrokeTransparency = 0
TopText.BackgroundTransparency = 1
TopText.Position = UDim2.new(0.35,0,0.05,0)
TopText.Size = UDim2.new(0,300,0,40)
TopText.Font = Enum.Font.Fantasy
TopText.TextScaled = true

Button.Parent = Frame
Button.Text = "👾tự động win👾"
Button.Size = UDim2.new(1,0,0.5,0)
Button.Position = UDim2.new(0,0,0,0)
Button.BackgroundColor3 = Color3.fromRGB(60,60,60)
Button.TextColor3 = Color3.fromRGB(255,255,255)
Button.Font = Enum.Font.GothamBlack
Button.TextScaled = true

LoopButton.Parent = Frame
LoopButton.Text = "👾 Auto Loop: OFF"
LoopButton.Size = UDim2.new(1,0,0.5,0)
LoopButton.Position = UDim2.new(0,0,0.5,0)
LoopButton.BackgroundColor3 = Color3.fromRGB(80,20,20)
LoopButton.TextColor3 = Color3.fromRGB(255,255,255)
LoopButton.Font = Enum.Font.GothamBlack
LoopButton.TextScaled = true

-- Overlay tìm kiếm
local Overlay = Instance.new("TextLabel")
Overlay.Parent = ScreenGui
Overlay.Size = UDim2.new(1,0,1,0)
Overlay.BackgroundColor3 = Color3.fromRGB(0,0,0)
Overlay.BackgroundTransparency = 0.3
Overlay.Text = " Hãy đợi một tí...\nVoid_GUI👾 đang tìm đích!"
Overlay.TextColor3 = Color3.fromRGB(0,255,255)
Overlay.Font = Enum.Font.Arcade
Overlay.TextScaled = true
Overlay.Visible = false

-- Cảm ơn 7 màu
local ThankYou = Instance.new("TextLabel")
ThankYou.Parent = ScreenGui
ThankYou.Size = UDim2.new(1,0,0.2,0)
ThankYou.Position = UDim2.new(0,0,0.4,0)
ThankYou.BackgroundTransparency = 1
ThankYou.Text = "🎩 Cảm ơn đã sử dụng 🇻🇳"
ThankYou.Font = Enum.Font.Arcade
ThankYou.TextScaled = true
ThankYou.Visible = false

-- Hiệu ứng 7 màu
local colors = {
    Color3.fromRGB(255,0,0),
    Color3.fromRGB(255,127,0),
    Color3.fromRGB(255,255,0),
    Color3.fromRGB(0,255,0),
    Color3.fromRGB(0,0,255),
    Color3.fromRGB(75,0,130),
    Color3.fromRGB(148,0,211)
}
task.spawn(function()
    local i = 1
    while task.wait(0.2) do
        ThankYou.TextColor3 = colors[i]
        i = i + 1
        if i > #colors then i = 1 end
    end
end)

-- Hàm tìm đích universal
local function FindGoal()
    Overlay.Visible = true
    task.wait(1.5)

    local plr = game.Players.LocalPlayer
    local char = plr.Character or plr.CharacterAdded:Wait()
    local root = char:FindFirstChild("HumanoidRootPart")
    if not root then return end

    local finishPart = nil
    local farthestDist = 0

    for _, obj in ipairs(workspace:GetDescendants()) do
        if obj:IsA("BasePart") then
            local name = string.lower(obj.Name)
            if string.find(name,"finish") or string.find(name,"end") or string.find(name,"goal") or string.find(name,"win") then
                finishPart = obj
                break
            end

            local dist = (obj.Position - root.Position).Magnitude
            if dist > farthestDist then
                farthestDist = dist
                finishPart = obj
            end
        end
    end

    Overlay.Visible = false
    if finishPart then
        root.CFrame = finishPart.CFrame + Vector3.new(0,5,0)
        ThankYou.Visible = true
        task.wait(1.5)
        ThankYou.Visible = false
    else
        warn("⚠ Không tìm thấy đích nào!")
    end
end

-- Nút Auto Win
Button.MouseButton1Click:Connect(function()
    FindGoal()
end)

-- Auto Loop fix
local autoLoop = false
local loopThread

LoopButton.MouseButton1Click:Connect(function()
    autoLoop = not autoLoop
    if autoLoop then
        LoopButton.Text = "🔁 Auto Loop: ON"
        LoopButton.BackgroundColor3 = Color3.fromRGB(20,80,20)

        loopThread = task.spawn(function()
            while autoLoop do
                pcall(function()
                    FindGoal()
                end)
                task.wait(3) -- delay giữa mỗi lần loop
            end
        end)
    else
        LoopButton.Text = "🔁 Auto Loop: OFF"
        LoopButton.BackgroundColor3 = Color3.fromRGB(80,20,20)
        autoLoop = false
    end
end)
